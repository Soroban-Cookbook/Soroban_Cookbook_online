import React,{useState} from 'react';const KEY='bm';export default function B(){const [items,setItems]=useState(JSON.parse(localStorage.getItem(KEY)||'[]'));const toggle=()=>{const p=location.pathname;const n=items.includes(p)?items.filter(x=x!==p)[...items,p];localStorage.setItem(KEY,JSON.stringify(n));setItems(n)};return <button onClick={toggle}>{items.includes(location.pathname)?'Y':'N'</button>}