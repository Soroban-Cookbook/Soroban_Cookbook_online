---
time: 10
sidebar_position: 2
title: Hello World Storage
description: Minimal Soroban contract with instance storage — pattern template example.
image: /img/soroban-social-card.png
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import { CodeSnippet } from '@site/src/components/CodeSnippet';
import { CodePreview } from '@site/src/components/CodePreview';
import { PatternCustomizer } from '@site/src/components/PatternCustomizer';

<PatternMeta
  slug="hello-world"
  difficulty="beginner"
  category="Storage"
  status="stable"
  lastReviewed="March 2026"
/>

## Overview

This page demonstrates the **pattern detail template**: metadata header, prerequisite and security sections, tabbed code, and related patterns. Use it as a starting point when adding new contract patterns to the cookbook.

<PatternSection id="prerequisites" title="Prerequisites">

- Rust stable and `cargo`
- Soroban CLI installed ([setup guide](/docs/getting-started/setup))
- Basic familiarity with Rust modules and `soroban_sdk` types

</PatternSection>

<PatternSection id="implementation" title="Implementation">

Read instance storage and return a greeting string. This is the smallest useful contract for bootstrapping tests and deployments.

<PatternCallout variant="info" title="When to use">

Use this pattern to verify tooling, ledger integration, and storage before layering tokens or authorization.

</PatternCallout>

<CodeSnippet
  collapseAt={12}
  tabs={[
    {
      label: 'Contract',
      fileName: 'hello-world/src/lib.rs',
      language: 'rust',
      highlightLines: [1, 7, 8],
      code: `#![no_std]
use soroban_sdk::{contract, contractimpl, Env, String};

#[contract]
pub struct HelloWorld;

#[contractimpl]
impl HelloWorld {
    pub fn hello(env: Env) -> String {
        env.storage().instance().get(&String::from_str(&env, "msg")).unwrap_or(
            String::from_str(&env, "Hello, Soroban!"),
        )
    }
}`,
    },
    {
      label: 'Test',
      fileName: 'hello-world/src/test.rs',
      language: 'rust',
      highlightLines: [1, 5, 7],
      code: `use soroban_sdk::{testutils::Storage, Env, String};

#[test]
fn hello_default() {
    let env = Env::default();
    let contract_id = env.register(HelloWorld, ());
    let client = HelloWorldClient::new(&env, &contract_id);
    assert_eq!(client.hello(), String::from_str(&env, "Hello, Soroban!"));
}`,
    },
  ]}
/>

<PatternCallout variant="info" title="Code with Comments">

The example below demonstrates the new comment toggle feature. Click "Hide comments" to see the code without detailed annotations:

</PatternCallout>

<CodeSnippet
  code={`#![no_std]
use soroban_sdk::{contract, contractimpl, Env, String};

// Define the contract struct that will hold the implementation
#[contract]
pub struct HelloWorld;

// Implement contract functionality
#[contractimpl]
impl HelloWorld {
    // Main entry point: returns a greeting message from storage
    pub fn hello(env: Env) -> String {
        // Retrieve the stored message from instance storage
        // If not found, return default greeting
        env.storage().instance().get(&String::from_str(&env, "msg")).unwrap_or(
            String::from_str(&env, "Hello, Soroban!"),
        )
    }
}`}
  language="rust"
  defaultShowComments={true}
  filename="hello-world"
  showDownload={true}
/>

## Live Code Preview

The `CodePreview` component renders read-only code with full syntax highlighting and a copy-to-clipboard button. It uses Prism under the hood, so keywords, types, and strings are color-coded automatically.

<CodePreview
  language="rust"
  title="hello-world/src/lib.rs"
  code={`#![no_std]
use soroban_sdk::{contract, contractimpl, Env, String};

#[contract]
pub struct HelloWorld;

#[contractimpl]
impl HelloWorld {
    pub fn hello(env: Env) -> String {
        env.storage().instance().get(&String::from_str(&env, "msg")).unwrap_or(
            String::from_str(&env, "Hello, Soroban!"),
        )
    }
}`}
/>

## Try it yourself

Customize the default greeting message to see how it changes the contract code:

<PatternCustomizer
  title="Customize Greeting"
  description="Change the default greeting message that the contract returns when no value is stored."
  fields={[
    {
      name: 'greeting',
      label: 'Default Greeting',
      type: 'text',
      value: 'Hello, Soroban!',
      defaultValue: 'Hello, Soroban!',
      placeholder: 'Enter your greeting message',
      description: 'This text will be returned when no message is stored in contract storage',
    },
  ]}
  codeTemplate={`#![no_std]
use soroban_sdk::{contract, contractimpl, Env, String};

#[contract]
pub struct HelloWorld;

#[contractimpl]
impl HelloWorld {
    pub fn hello(env: Env) -> String {
        env.storage().instance().get(&String::from_str(&env, "msg")).unwrap_or(
            String::from_str(&env, "{{greeting}}"),
        )
    }
}`}
  language="rust"
/>

</PatternSection>

<PatternSection id="security" title="Security considerations">

- **Storage scope**: Instance storage is shared across all invocations; never store per-user secrets without namespacing by `Address`.
- **Upgrade surface**: If you add admin functions later, protect them with `require_auth` and explicit roles.
- **Re-entrancy**: Not applicable to this minimal example; review when adding cross-contract calls.

<PatternCallout variant="warning" title="Production checklist">

Before mainnet, add thorough tests, metering review, and an explicit data migration plan if storage layout changes.

</PatternCallout>

</PatternSection>

<PatternSection id="related" title="Related patterns">

- [Pattern library overview](/docs/patterns/overview) — navigation hub
- [Storage concepts](/docs/concepts/storage) — deeper dive into Soroban storage
- [First contract tutorial](/docs/getting-started/first-contract) — step-by-step build

</PatternSection>
