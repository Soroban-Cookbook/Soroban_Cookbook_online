[package]
license = "MIT"
name = "upgradeable"
version = "0.1.0"
edition = "2021"
description = "Soroban example admin-controlled upgradeable contract."
keywords = ["soroban", "example", "upgradeable"]

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
soroban-sdk = "27.0.3"

[dev-dependencies]
soroban-sdk = { version = "27.0.3", features = ["testutils"] }
